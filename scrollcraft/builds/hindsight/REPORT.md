# Scroll-craft delivery

Applied to the existing Hindsight prototype at http://127.0.0.1:8767/.

Brief: self-authored from the established visual direction and the user's original creative delegation. Approved hero wording, static head asset remain intact. The separate 3D-model task was subsequently cancelled.

Grammar: Evidence trail. Four families: independent hero parallax, one-time section entrance, boundary line reveal and a sticky real comparison receipt. See BRIEF.md for why the eight stock grammars do not fit the approved existing page unchanged. Fingerprint gate: empty registry, no previous row to clear.

Signature: the sidebar's current audience/price and frozen baseline follow the actual application state. It does not claim that scrolling completes research. Method now precedes the final decision note/export. No values change merely because of scroll.

Intended feeling: curiosity → recognition → scrutiny → agency → understanding → resolve. Manual contact-sheet review: restrained opening and quiet evidence sections; strongest useful change comes from the operable price/baseline comparison. The baseline control remains beside price, and the final note/export resolves the page. This is deliberately a working demo rather than a filmic landing-page rebuild. No added empty pinning or scroll hijacking.

Verification: 48 sampled positions in each of desktop 1440×900, mobile 390×844, compact 360×640 and reduced-motion desktop: 192 states total. No horizontal overflow or JavaScript page errors. Baseline/receipt updates, one-change comparison, final section order, note export and keyboard focus passed in each mode. Contact sheets and machine-readable reports are in lab/. Headings are introduced once; reduced-motion content remains visible.

Tooling limits: skill doctor ran; FFmpeg is unavailable, so video encoding is unused and contact sheets were assembled with Pillow. The stock skill screenshot harness failed when its contrast-checker tried to inject inline CSS forbidden by the app's CSP; its initial frames are retained. The replacement Playwright check kept the production CSP active, disabled native pointer lock/capture, sampled the final page and tested controls. No full automated composited-contrast audit or real phone test is claimed.

No assets were generated. The skill engine files are copied unchanged. Page-specific choreography lives in product/prototype/static/hindsight-scroll.js and hindsight-scroll.css. Serving changes are four explicit static routes in server.py. Reuse these wrapper-level changes for the approved static head; no model rotation code was added here.
